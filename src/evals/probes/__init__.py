"""Ablation probes. One file per probe; each satisfies ``evals.interfaces.Probe``.

Probes:
    * order    — order-of-actions ablation
    * role     — role ablation
    * platform — platform-policy ablation
    * persona  — persona ablation
"""
